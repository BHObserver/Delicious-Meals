export function calculateEquation1(observedTime, allowances) {
    // Calculate standard time (ST) as the sum of observed time and allowances
    const standardTime = observedTime + allowances;

    return standardTime;
}